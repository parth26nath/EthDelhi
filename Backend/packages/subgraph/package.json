{
  "name": "@hpv-warriors/subgraph",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hpv-warriors/subgraph",
    "create-local": "graph create --node http://localhost:8020/ hpv-warriors/subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ hpv-warriors/subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hpv-warriors/subgraph"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.60.0",
    "@graphprotocol/graph-ts": "0.31.0"
  }
}