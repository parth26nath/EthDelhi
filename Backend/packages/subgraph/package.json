{
  "name": "subgraph",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hpv-warriors",
    "create-local": "graph create --node http://localhost:8020/ hpv-warriors",
    "remove-local": "graph remove --node http://localhost:8020/ hpv-warriors",
    "clean": "rm -rf build generated"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}